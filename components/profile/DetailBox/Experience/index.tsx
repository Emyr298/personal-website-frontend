import React, { useState } from 'react';
import Image from 'next/image';

type Props = {};

export const Experience : React.FC<Props> = ({}) => {
  return (
    <div>
      Experience
    </div>
  );
};
